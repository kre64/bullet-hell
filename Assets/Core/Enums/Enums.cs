
public enum WeaponType { Pistol, Shotgun, Lasergun }

public enum HeartStatus { Empty = 0, Half = 1, Full = 2 }

public enum GameState { MainMenu, InGame, ExitGame, GameOver }

public enum Scenes { MainMenu = 0, Game = 1 }
